# NET Feed — is the receiver actually getting the samples? The other screens in
# this section will count bursts, frames and devices; every one of those counts
# is a lower bound unless this one is on the same bench. Design section 13.2.
#
# It exists as a screen of its own because at this point it is the only thing
# the section measures. Once the survey lands, the panel travels with it and
# this layout can go.

section = "net"
slot    = 2
title   = "Feed"
blurb   = "what the receiver missed"

panels = [
    { name = "header",            position = "top", height = 5 },
    { name = "net_decode_health", position = "body" },
    { name = "log",               position = "bottom", height = 5 },
    { name = "footer",            position = "bottom" },
]
